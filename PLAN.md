# Salon Booking App - Agency Deployment Checklist

## Phase 1: Environment & Auth
- [x] Initialize Next.js project (scaffolded fresh, no template repo available).
- [x] Install core libraries: `@supabase/supabase-js`, `@supabase/ssr` (for httpOnly cookie sessions), `zod`, `browser-image-compression`.
- [x] Verify TypeScript strict mode (`tsconfig.json` already has `"strict": true` from scaffold).
- [x] Connect the client's specific Supabase keys to `.env.local` (real project: `loiubamyiiqzmmrpddpc`). `.env.local` is gitignored. Verified live: `next dev` boots against the real project with no client-construction errors, and `/admin/login` renders correctly (previously crashed on missing keys).
- [x] Create the Admin Login interface (`/admin/login`), protected `/admin` route via middleware, and sign-out action. Verified: typecheck clean, lint clean, dev server boots, and `/admin/login` fails at exactly the expected point (missing Supabase keys) rather than a code error.

## Phase 2: Database & Security
- [x] Create `services`, `bookings`, and `gallery` tables — migration applied to the real project. Verified live via the REST API (anon key): all 3 tables exist and respond correctly.
- [x] Enforce Row Level Security (RLS) on all tables — verified live: anonymous requests to `bookings` correctly return `[]` (no public SELECT policy), and anonymous inserts to `services` are correctly rejected (only the admin can write).
- [x] Storage RLS policies + the `gallery` bucket — bucket created and confirmed **public** (required for `getPublicUrl()` to actually serve images to anonymous visitors; caught this by testing the public URL route directly — initial private setting returned "Bucket not found", now correctly returns "Object not found" for a nonexistent file).
- Added `supabase/seed.sql` — sample services, run against the real project for testing.

## Phase 3: Frontend & Theming
- [x] Set up brand color tokens in `globals.css` (CSS variables + Tailwind v4 `@theme`) so re-theming a client is a one-file edit. Currently placeholder monochrome values from the BladeHub reference.
- [x] Build public-facing UI: header (with mobile menu), hero, services list, and booking form with a custom calendar + time-slot picker. Verified in-browser at desktop and mobile widths; full booking flow (validate → pick service/date/time → submit) tested end-to-end.
- [x] Build Gallery photo grid, Team carousel, and Testimonial slider (mock/static content — no `staff` or `testimonials` table exists; not database-backed). Newsletter signup intentionally skipped (out of PWA/booking scope). Verified in-browser: gallery grid, team carousel scroll buttons, and testimonial dot navigation all working.
- [x] Implement client-side image compression (WebP conversion). `src/lib/image-compression.ts` wraps `browser-image-compression` with `fileType: "image/webp"`, `initialQuality: 0.85`, `maxWidthOrHeight: 1200`. Verified with a real 1920×1200 JPEG (542KB) via a temporary unprotected test route: correctly compressed to a 1200×750 WebP file at 55KB (~90% smaller), aspect ratio preserved. Test route and sample asset removed after verification.

## Phase 4: Integration
- [x] Connect the booking form submission to Supabase. `submitBooking` Server Action (`src/components/booking/actions.ts`): re-validates with Zod server-side, looks up the service server-side (never trusts client-supplied price), inserts into `bookings` with the name/price snapshot columns. Added a hand-written `Database` type (`src/lib/supabase/database.types.ts`) so all Supabase queries are properly typed (no `any`), matching the strict-mode requirement.
- [x] Implement the WhatsApp Deep Link redirect upon successful booking insertion (`redirect()` to `https://wa.me/971501404437?text=...` with the "*New Booking Request!* ✂️" confirmation message template).
- [x] Real UAE WhatsApp number wired in (`siteConfig.contact.whatsappNumber`), and `siteConfig.timezone = "Asia/Dubai"` added — added `src/lib/timezone.ts` so booking date/time is parsed and stored as the correct UTC instant regardless of server timezone, and displayed back correctly in Dubai time. Verified with a standalone script: 2026-09-16 14:00 Dubai → `2026-09-16T10:00:00Z` UTC, formats back to 02:00 PM. Fixed-offset (+04:00) approach — correct since Dubai has no DST; would need a real IANA-aware library if reused for a DST-observing client.
- [x] Found and fixed a real gap while testing with live keys: the homepage was still importing mock service/gallery data — `submitBooking` was wired correctly, but nothing on the public site ever fetched real data, so booking would have silently referenced non-existent mock service IDs. `src/app/page.tsx` now fetches real `services` and `gallery` from Supabase; `GallerySection` now renders real uploaded images (previously always rendered the placeholder block regardless of `image_url`).
- [x] **Verified fully end-to-end with real data**: submitted a real booking (name/phone/Classic Cut/Sept 17 10:00 AM) through the live site — correctly redirected to `wa.me` with the exact pre-filled confirmation message, service price/name pulled live from the database.

## Phase 5: Admin Analytics & Booking Management
- [x] KPI cards: Total Bookings (Today / This Week / This Month, by appointment date in salon-local time), Estimated Revenue (confirmed + completed only), Popular Services (top 5 by `service_name_snapshot`), Booking Status Breakdown.
- [x] Bookings Overview: filterable table (status + date, via URL search params — no client JS needed) built as a Server Component.
- [x] Action Controls: one-click status toggles (Confirm / Complete / Cancel) per booking, each a tiny bound Server Action form (`updateBookingStatus`), with `revalidatePath` to refresh the dashboard.
- [x] Admin image upload UI (`/admin/gallery`): select → live client-side WebP compression with progress + before/after size preview → optional caption → upload to the `gallery` Storage bucket + insert into the `gallery` table (`uploadGalleryImage` Server Action). Also shows a read-only grid of currently-uploaded images. Linked from the main dashboard ("Manage Gallery"). Deliberately did not add delete/reorder management UI — not part of what was asked; flagging as a natural next step if wanted.
- [x] **Verified fully end-to-end with real data** (login/dashboard/gallery upload done by the user directly, since I don't authenticate into apps myself — everything else cross-checked by me via the read-only anon API): admin login works, the test booking appeared in the dashboard, status update ("Confirm") worked, and two real photos were uploaded through `/admin/gallery`. I independently confirmed both: `gallery` table has 2 rows, both filenames end in `.webp` (compression pipeline really ran on real uploads, not just the test route), both public URLs return `200 image/webp`, and both render correctly on the live public homepage (`complete: true`, real pixel dimensions, both ≤1200px per the compression setting).
- Note: KPIs are computed by fetching all bookings and reducing in JS (fine for a small single-location salon); if the dataset grows large, this should move to DB-side aggregation queries instead.

## Fully complete and verified end-to-end against the live Supabase project
Every planned feature is built, wired to real data, and verified working — not just typechecked. Nothing left blocking. Two small things worth knowing about, not blocking anything:
1. No admin UI exists yet to add/edit/delete `services` — for now that's done directly in the Supabase Table Editor (or by editing `supabase/seed.sql`-style inserts). Wasn't part of any phase's scope; flagging as a natural next feature if wanted.
2. No delete/reorder UI for gallery images (noted back in Phase 5) — same story.

## Phase 6: Admin Content Editing (CMS)
Designed in Stitch first ([blueprint](https://claude.ai/code/artifact/c781d280-0896-47d9-a075-a5a928e9d3a3)); the user supplied a zip with all 8 Stitch screens + a design system doc ("Atelier" — strict black/white "Utilitarian Modernism," Archivo Narrow + Inter, sharp 4px shapes, zero shadows). Adopted the visual language; deliberately dropped everything Stitch invented that doesn't correspond to real features (multi-stylist chair/station management, product inventory alerts, SKU codes, fake "Stripe POS" sync status, CSV export, "Publish Storefront" workflow) — scoped to real data only. New admin visual language applies only inside `/admin/*`; the public site is untouched.

- [x] **Admin shell rebuilt**: moved existing admin routes into a `(shell)` route group with a shared persistent sidebar (`AdminSidebar`) + scoped `next/font/google` loading of Archivo Narrow/Inter, so the new theme doesn't leak into the public site's tokens. New admin-specific design tokens added to `globals.css` (`--admin-*`), separate from the public site's tokens. Existing Dashboard restyled to match.
- [x] **Services Manager built** (`/admin/services`): table (name/price/duration/description) + a slide-over drawer for add/edit, with the same image-compression-preview pattern as Gallery. Full CRUD wired to the real `services` table via Server Actions with Zod validation.
- [x] **Gallery Manager enhanced** (`/admin/gallery`): added inline caption editing and delete (removes both the DB row and the actual Storage file — recovers the storage path from the stored public URL since we don't store it separately).
- [x] **All of the above verified against the real Supabase project**, not just typechecked: edited a real service's description and confirmed it persisted to the DB and appeared live on the public site; edited a gallery caption and confirmed via the DB; deleted a gallery image and confirmed both the DB row and the actual Storage object were gone (verified via the Storage list API, since a direct URL fetch was misleadingly still returning 200 from a 1-hour CDN cache — not a real bug). Reverted all test edits back to clean data afterward.
- [x] **Dashboard restyled to match the Stitch mockup's card/table treatment, using only real data.** The mockup itself showed a full multi-stylist chair-management system (stylist/chair assignment, product inventory alerts, color formula notes, client retention %, a reschedule-request queue, manual appointment creation) that has no equivalent anywhere in our schema — confirmed with the user this meant "match the visual style," not build that whole feature set, before touching anything. Adopted: icon-labeled KPI cards, a real derived "Avg ticket" stat on the revenue card, a genuinely data-driven status-proportion bar (not a fake progress bar), and avatar-initials + status-pill styling on the bookings table. Left out everything with no real backing data. Verified in-browser against live data.
- [x] **Site Settings built** (`/admin/settings`, 4th sidebar tab) — resolves the "deferred" architecture question: added a new `site_settings` table (single row) via an additive migration (`supabase/migrations/20260915090000_site_settings.sql` — the initial schema was already live, so this ships separately rather than editing an applied migration). Four sections matching the Stitch design: Business Info, Working Hours, Social Links, Brand Color — dropped the mockup's fictional "sync status" bar and fake preview chips, kept a real live color preview instead.
  - **Fully rewired the public site to read real settings, not the static file** — Header, Hero, Footer, root metadata, and the booking form's WhatsApp redirect all now call `getSiteSettings()` (React `cache()`-deduped per request) instead of importing `site-config.ts` directly. `site-config.ts` now serves only as the fallback default before the migration is applied.
  - **Fixed the long-flagged Working Hours duplication**: previously two separate hardcoded structures (footer display strings + calendar numeric hours) that had to be kept in sync by hand. Now one real source of truth (`working_hours` JSONB) drives both the footer's day-by-day display *and* the booking calendar's open-day/time-slot logic — the calendar (`src/lib/calendar.ts`) and its picker components now take working hours as a parameter instead of importing a static config.
  - **Brand Color is genuinely live**, not just stored: found that nothing on the public site actually referenced an accent token yet (it was 100% hardcoded monochrome), so wired it into the real "Book Now" CTAs (header, hero, booking form) via a server-rendered `<style>` override in the root layout, with an auto-computed contrasting text color (so an admin can't accidentally pick a color that makes button text unreadable). Changed the default accent from near-black to white after realizing black-on-black would make the button invisible against the site's already-dark header/hero — caught before it shipped.
  - **Timezone deliberately excluded** from this screen — flagged to the user rather than silently building it: the booking date/time math uses a hardcoded Dubai (+04:00) offset directly in code, not driven by any config value, so a timezone field here would be fake.
  - **Found and fixed a real client/server boundary bug while testing**: the settings form (a Client Component) did a runtime import of a color-contrast helper that lived in the same file as the server-only Supabase fetch — pulling the whole module, `next/headers` included, into the client bundle and crashing the build. Split the pure helper into its own file (`src/lib/color.ts`).
  - **Migration applied and fully verified live against the real database — the whole loop actually works**: changed the tagline and brand color (to blue) through the real admin UI, confirmed both persisted to the DB, then confirmed the public site picked them up live — the new tagline appeared in the hero/footer, and all three CTA buttons (header, hero, booking form) actually turned blue with correctly-computed white text. Ran a full real booking through to WhatsApp afterward to confirm `submitBooking`'s extra settings fetch didn't break anything — correct WhatsApp number, correct message, correct redirect. Reverted both test values back to clean afterward.
  - **Found and fixed a second real bug during this test**: `updated_at` wasn't changing on save — the column's `DEFAULT now()` only fires on `INSERT`, never on `UPDATE`, so it silently stayed frozen at the seed timestamp forever. Now set explicitly in the Server Action; confirmed fixed (the revert save produced a fresh timestamp).
  - Note: a few dev-server cache/HMR corruption errors showed up mid-testing (stale webpack manifests, one bad RSC-bundle reference) — all resolved by clearing `.next` and restarting; none were real application bugs, confirmed by each one going away on a clean restart with no code changes.
- [x] **Still deferred — Hero Section, Team, Testimonials**: Hero's headline/tagline are now covered by Site Settings; a dedicated background-image upload for the hero itself remains static. Testimonials remain static/mock (unchanged, out of scope). **Team is resolved — see Phase 7.**

## Phase 7: Real Staff, Per-Stylist Pricing/Schedules, Booking-Flow Stylist Selection, and Reviews
Full plan at the time of writing: `C:\Users\Surface\.claude\plans\compiled-swimming-spark.md`. Triggered by the user asking to build the Stitch "Team" admin modal — investigation showed Team had no DB table at all, and reference screenshots of the actual desired booking experience (Fresha-style) revealed the real requirement was much bigger: per-stylist pricing, per-stylist schedules, stylist selection in the booking flow, and a full review system. Explicitly dropped from the Stitch mockup as fabricated: staff "tier" (Master/Senior/Associate), "station assignment", and "off today" status — none of these have any real backing.

- [x] **`staff` + `staff_services` tables** (migration `20260915100000_staff.sql`) — real stylists with name/role/photo/weekly `schedule` (JSONB, same shape as `site_settings.working_hours`) and `is_active` (soft-delete only — no hard-delete exposed in the admin UI). `staff_services` is the per-stylist service+price join (`custom_price` nullable — null means "use the base service price").
- [x] **`bookings` gains staff assignment + review tracking** (migration `20260915100100_bookings_staff_and_review.sql`): `staff_id`, `staff_name_snapshot` (same snapshot pattern as `service_name_snapshot`), `service_duration_snapshot` (a genuine gap in the original schema — nothing previously captured how long a booking occupies a stylist's calendar, needed for real conflict-checking), `review_token`, `reviewed_at`.
- [x] **`reviews` table + two `SECURITY DEFINER` RPC functions** (migration `20260915100200_reviews.sql`) — `get_booking_for_review`/`submit_review`, the first use of `supabase.rpc()` in this codebase. Chosen over a plain RLS policy (would require a blanket anon SELECT on `bookings`, leaking customer PII) or a service-role key (a new secret the user would have to generate) — the function's parameter list is the security boundary instead. `staff_ratings` view aggregates real average rating + review count per stylist.
- [x] **`site_settings.google_review_url`** (migration `20260915100300_site_settings_google_review_url.sql`) — shown to customers who leave a 4-5 star review.
- [x] **Staff admin CRUD** (`/admin/staff`, 5th sidebar tab) — modeled directly on the Services admin pattern (table + slide-over drawer), photo upload reuses the existing `gallery` storage bucket under a `staff/` prefix (no new bucket needed). New shared `WeeklyScheduleEditor` extracted from the Site Settings "Working Hours" block so both screens use the identical component instead of duplicate JSX.
- [x] **Public Team section is now DB-backed** — real stylists with a live `★ 4.9 (24 reviews)` badge once reviews exist (no badge shown for zero reviews, not a fake "0.0").
- [x] **Booking flow rewritten**: still one consolidated page (no multi-step wizard, per the user's explicit "reduce clicking fatigue" request) — service → stylist picker (defaults to "Any Professional" the moment a service is picked) → date/time (now a real server round-trip via a new `getAvailableSlots` Server Action, computing each stylist's own schedule minus their actual conflicting bookings) → name/phone → submit. "Any Professional" resolves to the cheapest available eligible stylist at the exact chosen slot, re-validated at submit time (not just trusting the earlier browse-time check), and silently falls through to the next-cheapest still-free stylist on a last-second race rather than hard-failing (a named-stylist conflict still hard-fails).
- [x] **Review system end-to-end**: admin bookings table gets a Stylist column and, per completed+unreviewed booking, a "Send Review" WhatsApp link (to the *customer's* number, normalized via a new `src/lib/phone.ts` UAE helper) or a "Reviewed" badge. Public `/rate/[token]` page (outside the admin middleware entirely) handles not-found/already-reviewed/not-completed/ready states and posts to `submit_review` via `src/app/rate/[token]/actions.ts`.
- [x] **Verified as much as possible pre-migration**: typecheck and lint clean across the whole project; booted a real dev server and confirmed the homepage, booking flow (service → stylist → date → "no slots" for an empty staff table), `/admin` dashboard (existing real bookings render correctly with the new Stylist/Review columns gracefully showing "—"), `/admin/settings` (new Google Review field, refactored Working Hours editor unchanged in behavior), and `/admin/staff` (graceful "table not found" error, matching the exact same pattern Services/Gallery used before their own migrations were applied) all render with zero console/server errors.
- [x] **4 migrations applied by the user and verified live** via the read-only anon REST API: `staff`/`staff_services`/`reviews` tables, `staff_ratings` view, both `get_booking_for_review`/`submit_review` RPC functions, and `site_settings.google_review_url` all confirmed responding correctly (a fake review token correctly returns `not_found`). `NEXT_PUBLIC_SITE_URL` added to `.env.local` (still needs setting in production when deployed). User has already added real stylists through `/admin/staff` (confirmed live on the public Team section). Still pending: a full live booking → completed → review round-trip.

## Phase 8: Hero Section Banner Slideshow
The Stitch nav design always showed a "Hero Section" admin tab, but it was never built — Site Settings only ever covered the headline/tagline text, and the hero itself was just a plain gradient background with no image upload anywhere. Prompted by the user asking where to upload homepage banners.

- [x] **`hero_slides` table** (migration `20260915110000_hero_slides.sql`) — `image_url`, optional per-slide `caption` (replaces the site tagline while that slide is showing; the user chose per-slide captions over a single fixed caption), `sort_order` for admin-controlled ordering. Images reuse the `gallery` storage bucket under a `hero/` prefix, same convention as `services/`/`staff/` — no new bucket needed.
- [x] **`/admin/hero`** (new sidebar tab, positioned after Site Settings) — upload + compress-to-WebP (same pattern as Gallery), inline caption editing, delete, and up/down reorder buttons that swap adjacent `sort_order` values.
- [x] **Public Hero is now a real rotating slideshow** (`src/components/home/hero.tsx`, now a Client Component) — autoplay every 6s with dot navigation (same UX pattern as the existing testimonial slider), falls back to the original plain gradient background when zero slides exist.
- [x] Typecheck/lint clean; verified live in-browser both before migration (graceful "table not found" on `/admin/hero`, gradient fallback on the homepage) — migration for this phase not yet applied by the user.
- [x] Migration applied by the user; verified live via the anon REST API and confirmed the user had already uploaded 2 real slides through `/admin/hero`.
- [x] **Fixed a real bug**: `deleteHeroSlide` never returned or surfaced an error, so a failed delete (DB error, RLS issue, etc.) would silently no-op with zero feedback — looking exactly like "delete doesn't work." Now returns `{error}`, surfaced in the grid UI. Verified live: deleted a real test slide (2 → 1) successfully.
- [x] **Fixed hero image sizing/cropping**: added `lg:min-h-[85vh]` (was a flat `70vh` at every breakpoint) and explicit `object-center`; added a dedicated `HERO_IMAGE_COMPRESSION_OPTIONS` (1920px max width, was reusing Gallery's 1200px cap — too small for a full-bleed banner) with a hard `maxSizeMB: 2` cap; added "Recommended size: 1920×1080px (16:9), max 2MB, keep subjects centered" helper text in the uploader.
